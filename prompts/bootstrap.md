# Bootstrap Prompt

Use this repository as a local-first project named After 365.

Build and maintain a small, inspectable system that looks back exactly 365 days, identifies analytically useful events, stores structured results in SQLite, and renders daily Markdown outputs.

Follow the repository guardrails in `AGENTS.md`, the editorial rules in `docs/editorial-guidelines.md`, and the schema contract in `docs/schema.md`.
